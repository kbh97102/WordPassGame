<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    //TODO Change Image
    <ImageView
        android:id="@+id/computer_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@drawable/ic_launcher_foreground"
        />

    <TextView
        android:id="@+id/textview_computer_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text="@string/computer_name"
        app:layout_constraintStart_toEndOf="@id/computer_image"
        app:layout_constraintTop_toTopOf="parent"
        />

    <TextView
        android:id="@+id/textview_computer_word"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/textview_computer_name"
        app:layout_constraintStart_toEndOf="@id/computer_image"
        android:textSize="40sp"
        android:text="testText"
        />

</androidx.constraintlayout.widget.ConstraintLayout>